//! Evaluation & benchmarking — Phase 4 (see docs/ROADMAP.md).
//!
//! Plan:
//! * industry benchmark adapters plus MCW-native H/R/D/M series,
//! * the in-TUI survey flow (anchored ordinal rubrics, versioned so scores
//!   stay comparable — pre-registration friendly),
//! * new benchmarks as data: a benchmark is itself a durable bundle (task
//!   set + rubric + the graph that runs it), shareable like any graph,
//! * results as portable artifacts carrying both the data AND the process
//!   that produced the data (journal excerpts + spec hashes).
